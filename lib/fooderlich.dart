import 'package:flutter/material.dart';

class FooderlichScreen extends StatelessWidget {
  const FooderlichScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('fooderlich appbar'),
      ),
    );
  }
}
